// Bucle para construir la pirámide del 1 al 30
for (let i = 1; i <= 30; i++) {
    // Repetir el número 'i' tantas veces como el valor de 'i'
    let fila = i.toString().repeat(i);
    console.log(fila); // Mostrar cada fila en la consola
}
